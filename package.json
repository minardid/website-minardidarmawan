{
  "name": "website-minardidarmawan",
  "version": "1.0.0",
  "description": "Personal website for Minardi Darmawan",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"Build complete\" && exit 0",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase-tools": "^11.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true
}
